submit.addEventListener('click', função)
function Validar() {
    if(document.getElementById("username").value.length < 1 || document.getElementById("email").value.length < 1 || document.getElementById("password") .value.length < 1){
        alert ('Preencha todos os campo')
    }
    if(doc)
}